#include <stdio.h>
#include "./src/components/index.h"

int main() {
  /* steps */
  /* presentar la ui */
  /* para presentar la ui primero tiene que haber un estado inicial */
  input();

  return 0;
}
